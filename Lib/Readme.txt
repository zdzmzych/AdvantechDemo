Obsługa zrealizowana jest poprzez driver biodaq.dll (instalowany w c:\windows\system32)

Dostęp do funkcji drivera zrealizowany jest za pomocą bdaqctrl.h.
Uwaga: aby skompilować bibliotekę pod CVI należy wyłączyć opcję:
Options->Build options->Require function prototypes 